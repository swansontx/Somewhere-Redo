# Somewhere (Redo)

This repository is a fresh start (initial import) created from a working backup snapshot on 2025-10-20.

Notes:
- This is the baseline that successfully builds in the Simulator as of the import.
- The original repository (pre-redo) was archived at: `../Somewhere-archive-20251020-161129` on the same machine.
- Branch `goose/fix-build-20251020` contains the local fixes applied to get the project building.

How to run locally:
- Open the workspace: `Somerwhere V2.xcodeproj/project.xcworkspace`
- Select scheme `Somerwhere V2` and a simulator device, then build & run (Cmd-R).

Contact:
- Repository created and initial import by Goose assistant.
